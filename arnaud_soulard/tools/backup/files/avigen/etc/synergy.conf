# sample synergy configuration file
#
# comments begin with the # character and continue to the end of
# line.  comments may appear anywhere the syntax permits.

section: screens
	avigen:
	aggregation-platform:
end

section: links
	# aggregation-platform is to the right of avigen
	avigen:
		right = aggregation-platform

	# avigen is to the left of aggregation-platform
	aggregation-platform:
		left  = avigen
end

